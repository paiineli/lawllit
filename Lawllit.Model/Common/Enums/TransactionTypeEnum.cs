namespace Lawllit.Model.Common.Enums;

// Os valores inteiros são gravados na coluna "Type" do banco, não reordenar.
public enum TransactionTypeEnum
{
    INCOME = 0,
    EXPENSE = 1,
    INVESTMENT = 2,
}
